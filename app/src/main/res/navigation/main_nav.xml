<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_nav"
    app:startDestination="@id/breedsFragment">

    <fragment
        android:id="@+id/breedsFragment"
        android:name="to.chip.dogsgallery.view.fragment.CDGBreedsFragment"
        tools:layout="@layout/fragment_breeds">
        <action
            android:id="@+id/breeds_to_gallery"
            app:destination="@id/galleryFragment"
            app:enterAnim="@null"
            app:popEnterAnim="@null"
            app:exitAnim="@null"
            app:popExitAnim="@null">
            <argument
                android:name="breedName"
                app:argType="string"
                android:defaultValue=""/>
        </action>
        <argument
            android:name="breedName"
            app:argType="string"
            android:defaultValue=""/>
    </fragment>

    <fragment
        android:id="@+id/galleryFragment"
        android:name="to.chip.dogsgallery.view.fragment.CDGGalleryFragment"
        tools:layout="@layout/fragment_gallery">
    </fragment>
</navigation>